"""
DocuForge AI — Agent Memory Systems

This package manages three types of memory: short-term (current pipeline state),
long-term (persistent session database), and episodic (historical traces).
All database access lives in db_queries.py.
"""
